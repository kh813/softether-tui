# SoftEther vpncmd TUI ラッパー 仕様書

- ステータス: Draft v0.1
- 対象リポジトリ: softether-tui
- 作成日: 2026-07-29
- 参考資料 / vpncmd リファレンス:
  - 最新版: https://ja.softether.org/4-docs/1-manual/6
  - 旧バージョン: https://www2.softether.jp/en/vpn2/manual/web/6.aspx

## 1. 背景・課題

- SoftEther VPN Server は Linux 上で動かしつつ、実際の管理操作は Windows 用 GUI 管理ツール (SoftEther VPN サーバーマネージャー) を別途用意して行っている運用者が多いと考えられる。これは Linux/Mac 向けに提供されている `vpncmd` に以下のような課題があるためと推測される:
  - コマンド名・パラメータの補完が効かず、都度リファレンスを確認しながら入力する必要がある。
  - 対話式メニューの階層が深く、目的の設定にたどり着くまでの手順の見通しが悪い。
  - 一覧系コマンド (`UserList`, `SessionList` など) の出力が固定幅テキストで、複数対象の横断把握や絞り込みがしにくい。
  - Hub を切り替えるたびに `Hub` コマンドで選択し直す必要があり、複数 Hub / 複数サーバーの並行管理が煩雑。
  - 誤操作 (削除系コマンドなど) への確認が薄い。
- その結果、Linux サーバーの保守 (OS/パッケージ更新、ログ確認、SSH 経由の操作) は Linux 側で行いつつ、VPN Server 自体の設定変更のためだけに Windows GUI 管理ツールを起動するという「操作場所が分かれる」運用になりがちである。
- この課題を解決するため、`vpncmd` の TUI (Text User Interface) ラッパーを開発する。SSH 等で Linux サーバーに接続した端末上から、サーバーの保守作業と VPN Server の設定操作の両方を 1 箇所 (ターミナル) で行えるようにすることを狙う。
- 実装としては `vpncmd` をバックエンドとして呼び出しつつ、TUI でカーソル操作・一覧表示・確認ダイアログ等、GUI 管理ツール相当の操作性を提供するラッパーアプリケーションとする。

## 2. 目的

1. Linux サーバー上の SoftEther VPN Server / VPN Bridge を、SSH ログイン後の端末から GUI マネージャー相当の操作感で管理できるようにする。
2. サーバーの保守作業 (SSH 経由のコマンド操作) と VPN Server の設定・運用作業を同じターミナル上で行えるようにし、設定変更のためだけに別途 Windows GUI 管理ツールを起動する必要をなくす。
3. 複数の SoftEther サーバー (ローカル・リモート) をプロファイル切り替えで一元管理できるようにする。
4. 特に日常運用で頻度が高い「ユーザー/グループ管理」「セッション/ログ監視」「HUB/リスナー設定」を最優先で使いやすくする。
5. 破壊的操作 (削除・切断など) は確認ステップを挟み、事故を防ぐ。

## 3. スコープ

### 3.1 MVP スコープ (今回の実装対象)

- `vpncmd` の **VPN Server 管理モード (/SERVER)**、**VPN Bridge 管理モード (/BRIDGE)**、**VPN Client 管理モード (/CLIENT)** をラップする (VPN Client 対応は当初ロードマップでは将来項目としていたが、前倒しで着手した)。
- 複数サーバー (ローカル / リモート) への接続プロファイルを保存・切り替えできる。プロファイルはモード (Server/Bridge/Client) を問わず同じ仕組みで管理する。
- 対象機能は本仕様書 5 章の機能要件を参照。

### 3.2 非スコープ / 将来ロードマップ

以下は MVP には含めないが、将来的な拡張として設計上考慮しておく (完全に閉じた設計にしない):

- **VPN Tools モード (/TOOLS)** のユーティリティ系コマンド (証明書生成、ネットワークテストなど) の統合。
- VPN Client の証明書認証 (`AccountCertSet` 等)、NIC管理 (`NicList`/`NicCreate`/`NicDelete`)、信頼するCA管理 (`TrustCAList`等)、自動接続設定 (`AccountStartupSet`等)、接続設定のインポート/エクスポート (`AccountExport`/`AccountImport`) — vpncmd の詳細なパラメータ構文が未確認、または優先度が低いため見送り (詳細は `vpncmd_commands.md` 4章参照)。
- vpncmd 全コマンドの完全な網羅 (ロングテールなコマンドは都度追加)。
- Web UI 化、マルチユーザーでの同時ログイン管理、RBAC。

将来これらを追加する際にアーキテクチャ変更が最小で済むよう、「モード (Server/Bridge/Client/Tools)」を切り替え可能な抽象レイヤーとして設計している (6章参照)。

## 4. 想定ユーザー・ユースケース

- 想定ユーザー: SoftEther VPN サーバーを Linux 上で運用しているインフラ/ネットワーク担当者本人 (シングルユーザー運用が前提。マルチテナント管理 UI は想定しない)。
- 主なユースケース:
  1. SSH でサーバーに入り、TUI を起動して当該サーバー (localhost) に接続し、ユーザー追加・パスワード再設定を行う。
  2. 手元の PC から TUI を起動し、拠点ごとに異なるリモートの SoftEther サーバーに接続プロファイルを切り替えながら、Hub やリスナーの設定を確認・変更する。
  3. 特定 Hub の現在の接続セッション一覧をリアルタイムに近い形で監視し、不審なセッションを切断する。
  4. セキュリティログ / パケットログの保存設定を確認・変更し、直近のログをその場で閲覧する。
  5. 拠点間カスケード接続 (Bridge) のステータスを確認し、オフラインになっている接続を再接続する。

## 5. 機能要件

各機能は対応する主要 vpncmd コマンドを併記する (実装時の参照用。コマンド名は SoftEther のバージョンにより増減しうる)。vpncmd の全コマンド一覧と本アプリでの実装状況の対応表は [`vpncmd_commands.md`](./vpncmd_commands.md) を参照。

### 5.1 接続プロファイル管理

- プロファイルの一覧・追加・編集・削除。
- プロファイル項目: 表示名, ホスト, ポート (既定 443), 対象 Hub (任意), 管理者パスワード種別 (サーバー全体管理者 / Hub 管理者), 使用暗号化 (TLS), 証明書検証設定。
- パスワードは平文で設定ファイルに保存しない (7章参照)。
- 接続テスト (Server/Bridge は `ServerInfoGet`、Client は `AccountList` の呼び出しで疎通確認)。
- モード切替: 同一プロファイルに対して Server / Bridge / Client のどのモードで開くか選択 (それぞれ `/SERVER` / `/BRIDGE` / `/CLIENT` 相当で接続)。Client モードは Hub の概念を持たないため、接続後は 5.10 の VPN Client 管理画面に遷移する。

### 5.2 サーバーダッシュボード

- 接続後のトップ画面。サーバー種別・バージョン・稼働時間・Hub 数・総セッション数などのサマリを表示 (`ServerInfoGet`, `ServerStatusGet`)。
- Hub 一覧への導線、リスナー一覧への導線。

### 5.3 HUB 管理

- Hub 一覧表示 (種別: スタンダード / Extend / 状態オンライン・オフライン, 現在の接続数) (`HubList`)。
- Hub 作成・削除・有効化/無効化 (`HubCreate`, `HubDelete`)。
- Hub 詳細設定の閲覧・編集 (`HubGet`, `HubSet`): 最大セッション数, オンライン/オフライン切替, パスワードポリシー既定値など。
- SecureNAT 設定の有効化/無効化と状態確認、DHCP 配布設定 (`SecureNatEnable`, `SecureNatDisable`, `SecureNatStatusGet`, `SecureNatHostGet/Set`)。
- アクセスリスト (パケットフィルタ) の一覧・追加・削除・有効/無効切替 (`AccessList`, `AccessAdd`, `AccessDelete`, `AccessEnable`, `AccessDisable`)。

### 5.4 リスナー管理

- リスナー (ポート) の一覧・作成・削除・有効/無効切替 (`ListenerList`, `ListenerCreate`, `ListenerDelete`, `ListenerEnable`, `ListenerDisable`)。

### 5.5 ユーザー / グループ統合管理 (重点機能)

- **統合画面構成**: Hub詳細画面で `Users & Group` を単一の統合タブとして提供。上部に Users (ユーザー一覧)、下部に Groups (グループ一覧) を配置し、シームレスな上下スクロールおよび `u`/`g` ショートカットキーで即時切替可能。
- **ユーザー一覧表示**: 認証方式 (Password/Anonymous/Radius/Cert/SignedCert/NTLM)・ログイン回数・最終ログイン・有効期限・所属グループをテーブル表示し、`/` キーによる即時絞り込み検索に対応 (`UserList`)。
- **直感的な TUI フォーム UI**: Group 選択や Auth method 選択フィールドで `Enter` キーを押すことで、TUI 上に動的なプルダウン選択メニュー（オーバーレイ）が表示され、`↑/↓`・`Enter` で快適に選択可能。
- **アクセシビリティ・高コントラスト配色**: ダークモード環境において、`Save` や決定操作バッジに全盲・ロービジョンに配慮した高輝度エメラルド背景＋黒文字 (`Bold`) を採用。
- **ユーザー作成 & 認証方式拡充 (`UserCreate`)**:
  - パスワード認証 (`UserPasswordSet`)
  - 匿名認証 (`UserAnonymousSet`)
  - RADIUS 認証 + RADIUSエイリアス設定 (`UserRadiusSet /ALIAS`) ※RADIUSサーバー本体の設定は Hub 全体の `RadiusServerSet` で動作
  - 個別 X.509 証明書認証 (`UserCertSet /LOADCERT`)
  - 署名付き CA 証明書認証 (`UserSignedSet /CN /SERIAL`)
  - NT ドメイン / Active Directory 認証 (`UserNTLMSet /ALIAS`) ※認証先の AD / ドメインコントローラーは VPN Server が所属する Windows / AD ドメインに依存
- **ユーザー詳細閲覧・インライン編集** (`UserGet`, `UserSet` による氏名 `/REALNAME:`・備考 `/NOTE:`・グループ `/GROUP:` の変更、および `Auth Type` フィールドからのドロップダウン選択による認証方式の一括変更)。
- **パスワード再設定** (`UserPasswordSet`)。
- **アカウント有効期限設定** (`UserExpiresSet`): `2026/01/01` 形式に加え、`20261101` などの数字8桁省略入力を自動フォーマット。
- **ユーザー削除** (確認ダイアログ必須) (`UserDelete`)。
- **グループ一覧・作成・削除・編集・メンバー管理** (`GroupList`, `GroupCreate`, `GroupGet`, `GroupSet`, `GroupDelete`, `UserSet`):
  - **グループ詳細 (`GroupGet`)**: グループ属性 (Full Name / Description) の表示・編集に加え、所属ユーザー一覧 (Members) をインタラクティブ表示。
  - **直感的なメンバー解除 UI**: 各メンバー行に `[ ]` / `[x]` チェックボックスを配し、`Space` キーでトグル選択。1つ以上選択すると動的に `[ Remove ... ]` ボタンおよび `[ Clear Selection ]` ボタンが出現し、`←/→` や `↑/↓` でフォーカスを移動可能。フォーカス中のボタンのみに高輝度ハイライト（エメラルドグリーン背景＋黒文字）が動的に適用され、`Enter` で決定または選択解除が実行できる。
- **階層に応じた安全な終了保護 (Quit Confirm) と確認モーダルのキーボードナビゲーション**: Hub 概要・ダッシュボード画面では `q` キーで即時終了可能。一方、詳細サブ画面 (グループ詳細・ユーザー詳細・SecureNAT詳細など) では誤操作防止のため `q` キー押下時に終了確認ダイアログを表示。全ての確認ダイアログは `←`/`→`/`Tab`/`h`/`l` キーによるボタン間フォーカス移動に対応し、フォーカス中のボタンが高輝度ハイライト（エメラルドグリーン背景＋黒文字）表示され、`Enter` キーでフォーカス中のボタン（実行/キャンセル）を決定・実行可能。もちろん `y`/`n`/`Esc` ショートカットも併用可能。

### 5.6 セッション / ログ / SecureNAT 設定・監視 (重点機能)

- Hub 内の現在の接続セッション一覧をテーブル表示 (ユーザー名, 接続元 IP, 接続時刻, 通信量) (`SessionList`)。
- 一定間隔 (既定 5 秒、変更可) での自動リフレッシュ。
- セッション詳細表示 (`SessionGet`)。
- セッション切断 (確認ダイアログ必須) (`SessionDisconnect`)。
- SecureNAT 仮想ホスト設定および DHCP サーバー配布設定の閲覧・編集 (`SecureNatHostSet` による IP/Subnet Mask 編集、`DhcpSet` による配布 IP 範囲・ゲートウェイ・DNS 設定の編集)。
- セキュリティログ / パケットログの保存設定閲覧・変更 (`LogGet`, `LogEnable`, `LogPacketSaveType`, `LogSwitchType`)。
- 直近のログファイル内容をその場で閲覧 (tail 的なビュー、キーワードフィルタ)。ログ本体はサーバーのファイルシステム上にあるため、ローカル接続時はファイル直接参照、リモート接続時は `vpncmd` 経由で取得可能な範囲に限定することを明記する (完全なログ転送機能は非スコープ)。

### 5.7 カスケード接続管理 (Bridge 向け)

- カスケード接続 (拠点間 Hub-to-Hub 接続) の一覧・作成・削除 (`CascadeList`, `CascadeCreate`, `CascadeDelete`)。
- 接続状態確認・詳細表示 (`CascadeStatusGet`, `CascadeDetailGet`)。
- オンライン/オフライン切替 (`CascadeOnline`, `CascadeOffline`)。

### 5.8 ローカルブリッジ管理

- 物理 NIC / tap デバイスとのブリッジ設定一覧・作成・削除 (`BridgeList`, `BridgeCreate`, `BridgeDelete`)。

### 5.9 証明書管理 (簡易)

- サーバー証明書の現在の情報表示、証明書ファイルの差し替え (`ServerCertGet`, `ServerCertSet`)。
- 証明書生成そのもの (openssl 相当の操作) は非スコープ、既存ファイルの指定のみ対応。

### 5.10 VPN Client 管理 (`/CLIENT` モード)

Client モードのプロファイルは Hub の概念を持たないため、プロファイル選択画面から接続すると Hub 一覧を含むサーバーダッシュボード (5.2) ではなく、接続設定 (アカウント) の一覧を表示する専用画面に遷移する。

- 接続設定 (アカウント) の一覧表示 (`AccountList`)。
- 接続設定の作成: 接続名・接続先サーバー (ホスト:ポート)・接続先 Hub・認証方式 (Password / Anonymous) を指定 (`AccountCreate` + `AccountPasswordSet`/`AccountAnonymousSet`)。証明書認証は非スコープ (`AccountCertSet` のパラメータ未確認のため)。
- 接続設定の削除 (確認ダイアログ必須) (`AccountDelete`)。
- 接続の開始・切断 (`AccountConnect`, `AccountDisconnect`)。
- パスワード再設定 (`AccountPasswordSet`)。
- 非スコープ: NIC 管理 (`NicList`/`NicCreate`/`NicDelete`)、信頼する CA 証明書管理 (`TrustCAList` 等)、自動接続 (スタートアップ) 設定 (`AccountStartupSet`/`AccountStartupRemove`)、接続設定のインポート/エクスポート (`AccountExport`/`AccountImport`)。

## 6. アーキテクチャ

### 6.1 全体構成

```
+-----------------------------------------------------+
|                    TUI (Go / Bubble Tea)             |
|  画面遷移・状態管理・入力ハンドリング・確認ダイアログ |
+-----------------------------------------------------+
|              コマンド実行アブストラクション層         |
|  (Mode: Server / Bridge / [将来: Client / Tools])    |
+-----------------------------------------------------+
|         vpncmd 実行アダプタ (subprocess + parser)     |
+-----------------------------------------------------+
|             vpncmd バイナリ (別プロセス起動)          |
+-----------------------------------------------------+
|         SoftEther VPN Server / Bridge (対象ホスト)   |
+-----------------------------------------------------+
```

### 6.2 vpncmd との連携方式

- 対話式セッションを維持するのではなく、**各操作ごとに `vpncmd <接続先> /SERVER (or /BRIDGE) /HUB:<hub> /PASSWORD:<pw> /CMD:<command> <args>` を非対話コマンド実行し、標準出力をパースする**方式を基本とする。
  - 理由: 対話式プロセスの状態管理より単純で、タイムアウトやエラー時の回復がしやすい。
  - パスワードを引数に渡す方式は `ps` 等でプロセス一覧から見える懸念があるため、実装時は環境変数経由 (`VPNCMD_ADMIN_PASSWORD` 等 vpncmd がサポートする方式を確認の上採用) または標準入力経由でのパスワード受け渡しを優先する。
- 出力パース: vpncmd の `/CSV` オプション (CSV 出力モード) が利用可能なコマンドはそちらを優先してパースし、固定幅テキストのパースを避ける。CSV 非対応コマンドのみ固定フォーマットパーサーを個別実装する。
- 将来的な代替案 (本 MVP では採用しないが設計上排除しない): SoftEther が提供する **JSON-RPC 管理 API (HTTPS)** に直接接続する方式。テキストパースより堅牢だが「vpncmd のラッパー」という当初コンセプトから外れるため、v2 以降の検討事項として本仕様書に記録するのみとする。
- コマンド実行アブストラクション層は「モード (Server/Bridge/Client/Tools)」ごとにコマンドセットを切り替えられるようインターフェース化し、将来の Client/Tools モード追加時に TUI 側の画面遷移フレームワークを再利用できるようにする。

### 6.3 複数サーバー管理

- 接続プロファイルはローカル設定ファイル (例: `~/.config/softether-tui/profiles.yaml` 等、XDG Base Directory 準拠) に保存。
- 同時に開けるのは 1 サーバー (1 プロファイル) ずつを基本とし (MVP)、サーバー切り替えはトップ画面からのプロファイル選択で行う。複数サーバーの同時並行監視 (マルチペイン) は将来拡張。

### 6.4 技術スタック

- 言語: Go
- TUI フレームワーク: Bubble Tea (+ Lip Gloss でスタイリング、Bubbles で一覧/テーブル/フォームの部品を利用)
- 配布形態: シングルバイナリ。GitHub Actions + GitHub Releases でクロスコンパイル済みバイナリ (linux/amd64, linux/arm64, darwin/arm64, windows/amd64) を配布し、`curl` でのダウンロード実行に対応する (詳細は 11 章)。
- 対象 vpncmd: 実行環境に `vpncmd` バイナリ (SoftEther VPN Server または VPN Bridge に付属) が必要。本体には同梱しない。起動時に `vpncmd` の存在確認・バージョン確認を行う。

### 6.5 多言語対応 (i18n)

- **背景・課題**: UI 文言を日本語決め打ちで実装していたため、日本語ロケールが設定されていない (または UTF-8 未対応の) Linux サーバー上で実行すると、日本語文字列や罫線・記号 (`●`/`✕` 等) が文字化けし、実用に耐えないケースがあった。
- **対応方針**:
  - 起動時に表示言語を自動判定する。判定順は POSIX のロケール優先順位に倣い `LC_ALL` → `LC_MESSAGES` → `LANG` の順に環境変数を確認し、値が `ja` で始まる場合は日本語、それ以外の値が設定されていれば英語とする。いずれの環境変数も未設定の場合は英語をデフォルトとする。
    - 英語をデフォルトにする理由: 英語表示は基本的に ASCII 文字のみで構成できるため、ロケール・ターミナルの UTF-8 対応状況によらず文字化けしない、最も安全なフォールバックであるため。
  - `--lang en` / `--lang ja` コマンドライン引数で明示的に上書きできる。自動判定より優先する。
  - 対象範囲: TUI 上のラベル・見出し・タブ名・キー操作ヒント・ステータスメッセージ (作成/削除/成功/失敗などの動的メッセージを含む)。
  - 英語表示時は、罫線 (画面の枠) や状態記号 (`●`/`✕` 等)・矢印 (`↑/↓/←/→`) もアスキーセーフな代替表現に切り替え、Unicode 記号由来の文字化けも合わせて防ぐ。
  - vpncmd 自体が返すエラーメッセージ・出力内容 (`ServerInfoGet` の項目名など) は翻訳対象外とする。vpncmd 側のロケール設定に依存するため、本アプリの言語設定とは独立した挙動になる点を利用者に案内する。
- **非対応**: 3 言語以上への対応、ユーザーごとの言語設定の永続化 (毎回 `--lang` を指定するか、環境変数側で制御する想定)。

## 7. セキュリティ

- 管理者パスワードは設定ファイルに平文保存しない。OS のキーチェーン (macOS Keychain, Windows Credential Manager, Linux は Secret Service / `pass` 等) を優先的に利用し、非対応環境では起動時にプロンプト入力を求めるフォールバックとする。
- vpncmd へのパスワード引き渡しはプロセス引数を避け、標準入力または環境変数経由にする (6.2 参照)。
- リモート接続時は TLS を使用し、既定で証明書検証を有効にする。自己署名証明書運用の場合は「信頼する証明書のピン留め」をプロファイル単位で明示的に許可する UI を用意し、無条件の検証無効化はデフォルトにしない。
- 削除・切断など不可逆/破壊的な操作は、実行前に対象名を明示した確認ダイアログを必須とする。

## 8. UI/UX 設計方針

### 8.1 画面構成概要

1. プロファイル選択画面 (起動時)
2. サーバーダッシュボード (接続後のトップ)
3. Hub 一覧 → Hub 詳細 (タブ: 概要 / ユーザー / グループ / セッション / セキュリティログ / SecureNAT / アクセスリスト / カスケード)
4. リスナー管理画面
5. ローカルブリッジ管理画面
6. 各種確認ダイアログ (削除・切断など破壊的操作)、キーバインド一覧オーバーレイ (`?`)

画面遷移は基本的に「プロファイル選択 → ダッシュボード → Hub詳細 (タブ切替)」の一方向ドリルダウン + `Esc` で一段階戻る、をベースとする。

- **ウィンドウサイズ・レイアウト固定**: ターミナルの縦横サイズ（`WindowSizeMsg`）に追従し、全画面枠をターミナル上下幅いっぱいにフィットさせて表示する。
- **操作フィードバック (ステータスメッセージ) の上部表示**: ユーザー作成・削除・設定変更などの操作完了/エラーフィードバック（`Created user "user01"` 等）は、画面最下部ではなく**画面タイトルのすぐ下（コンテンツエリア上部）**に配置・表示する。
- **キー操作ガイドの最下部固定表示 (GNU nano / htop スタイル)**: 各画面下部の操作キー説明（`renderHelp`）は、中央コンテンツの行数に関わらず常にウィンドウ枠（下枠線）のすぐ上（画面最下部）に固定配置する。
- **共通 UI/UX デザイン標準 (インライン編集・一元保存 / 破棄ルール / トグル状態表示)**:
  - ユーザー・グループ・SecureNAT・アカウント等のすべての詳細設定画面において、標準的な UI 操作パターンを適用する。
  - `↑/↓`（または `j/k`）キーで画面内の全編集可能項目へ順番にフォーカス移動する。
  - フォーカス位置で `Enter` キーを押すことで、その場で値をインライン編集（または入力フォームを起動）できる。
  - **Enabled/Disabled（On/Off）トグル表示デザイン**: 現在適用されている有効な状態側は `[enabled]`（角括弧囲み＋目立つ強調カラー）でハイライト表示し、非選択の無効な状態側は `disabled`（角括弧なし＋暗いグレー）で表示する（例: 有効時 `[enabled]  disabled` / 無効時 `enabled  [disabled]`）。
  - **誤操作防止の確認ステップ**: SecureNAT、DHCP、Listener などの有効化/無効化を行う操作では、予期せぬパケット流出やサービス中断を防ぐため、Enter キー押下時に必ず明示的な確認ダイアログ（`confirmDialog`）をポップアップ表示し、ユーザーが `y` を押した場合のみ実行する。
  - 変更箇所が存在する場合（`dirty` 状態）は、直ちに項目横に `(変更あり)` タグが表示され、画面最下部のキーナビゲーションに `s: 保存 (Save)` / `c: 変更を破棄 (Cancel)` が提示される。
  - `s` キーで変更内容を `vpncmd` へ一括送信・保存し、`c` キー（または `Esc`）で変更を安全に一元破棄できる。
- **タブ切り替え操作**: `Tab` / `Shift+Tab` に加え、左右矢印キー（`←` / `→`）での直感的なタブ移動に対応する。
- **コンテキストに応じたキーハイライト**: 入力を受け付けるキー文字は周囲のテキストと異なるハイライト色（パステルパープル等）で表示し、操作可能なアクションを目立たせる。

### 8.2 画面ワイヤーフレーム (案)

以降はレイアウトイメージであり、実際の枠線・文字装飾は Lip Gloss での実装時に調整する。

**① プロファイル選択画面**

```
┌─ softether-tui v0.1.0 ────────────────────────────────────────────┐
│ 接続プロファイルを選択                                              │
├────────────────────────────────────────────────────────────────────┤
│ > Tokyo-Prod        vpn.example.jp:443       Server   ● 接続確認済み │
│   Osaka-Bridge      10.0.2.5:5555            Bridge    未確認        │
│   Local             127.0.0.1:443            Server    未確認        │
│                                                                      │
├────────────────────────────────────────────────────────────────────┤
│ ↑/↓ j/k:選択  Enter:接続  a:追加  e:編集  d:削除  t:接続テスト  /:検索│
└────────────────────────────────────────────────────────────────────┘
```

**② サーバーダッシュボード**

```
┌─ softether-tui ─────────────────── Tokyo-Prod (vpn.example.jp:443) ─┐
│ VPN Server v4.43-9799 Build10008 (Linux x64)  稼働時間: 42日6:12:03  │
├────────────────────────────────────────────────────────────────────┤
│ Hub数: 3   総セッション数: 27   リスナー: 2 (稼働中)                  │
│                                                                      │
│ NAME       STATUS   SESSIONS   USERS   GROUPS   TYPE                │
│ > Main       ● On       18         12       3     Standalone         │
│   Guest      ● On        9          5       1     Standalone         │
│   Staging    ○ Off       0          3       0     Standalone         │
├────────────────────────────────────────────────────────────────────┤
│ ↑/↓:選択  Enter:Hub詳細  l:リスナー  b:ローカルブリッジ  r:更新       │
│ p:プロファイル切替  q:終了                                           │
└────────────────────────────────────────────────────────────────────┘
```

**③ Hub 詳細 (タブ構成、例: 概要タブ)**

```
┌─ softether-tui ────────────────────── Tokyo-Prod / Hub: Main ───────┐
│[概要] ユーザー グループ セッション セキュリティログ SecureNAT ACL カスケード│
├────────────────────────────────────────────────────────────────────┤
│ 状態: オンライン        最大セッション数: 100 (無制限)                │
│ 作成日: 2019-04-02      管理者パスワード設定: あり                    │
│ SecureNAT: 有効         DHCP配布: 有効 (192.168.30.0/24)              │
├────────────────────────────────────────────────────────────────────┤
│ Tab/Shift+Tab:タブ切替  o:オンライン/オフライン切替  s:設定編集  Esc:戻る│
└────────────────────────────────────────────────────────────────────┘
```

**④ ユーザー一覧タブ (重点機能)**

```
┌─ softether-tui ─────────────────── Tokyo-Prod / Hub: Main / Users ───┐
│ 概要 [ユーザー] グループ セッション セキュリティログ SecureNAT ACL カスケード│
├────────────────────────────────────────────────────────────────────┤
│ 検索: hiro_                                            12件 / 全12件  │
│ NAME      FULL NAME    GROUP    AUTH TYPE  EXPIRES     LAST LOGIN     │
│ > hiroshi   Hiroshi K.   admins   Password   無期限      07-28 21:03   │
│   suzuki    Suzuki T.    users    Password   無期限      07-20 09:11   │
│   tanaka    Tanaka M.    users    Cert        2026-12-31  07-15 18:40  │
├────────────────────────────────────────────────────────────────────┤
│ ↑/↓:選択 Enter:詳細 a:追加 e:編集 p:パスワード再設定 d:削除 /:検索     │
│ Esc:戻る  Tab:タブ切替                                                │
└────────────────────────────────────────────────────────────────────┘
```

**⑤ セッション監視タブ (重点機能)**

```
┌─ softether-tui ─────────────── Tokyo-Prod / Hub: Main / Sessions ────┐
│ 概要 ユーザー グループ [セッション] セキュリティログ SecureNAT ACL カスケード│
├────────────────────────────────────────────────────────────────────┤
│ 自動更新: 5秒毎  最終更新: 2秒前                            18件      │
│ USER      SRC IP          CONNECTED     DURATION  TX/RX        TYPE   │
│ > hiroshi   203.0.113.10    07-29 09:12   3:41:02   120MB/980MB SSL-VPN│
│   suzuki    203.0.113.55    07-29 10:03   2:50:11    45MB/210MB L2TP  │
├────────────────────────────────────────────────────────────────────┤
│ ↑/↓:選択 Enter:詳細 x:切断 r:手動更新 +/-:更新間隔変更 /:検索         │
│ Esc:戻る  Tab:タブ切替                                                │
└────────────────────────────────────────────────────────────────────┘
```

**⑥ 破壊的操作の確認ダイアログ (モーダル)**

```
        ┌─ 確認 ─────────────────────────────────┐
        │ ユーザー "tanaka" を削除しますか?          │
        │ この操作は取り消せません。                  │
        │                                            │
        │        [ y: 削除する ]   [ n: キャンセル ]  │
        └────────────────────────────────────────┘
```

**⑦ キーバインド一覧オーバーレイ (`?`)**

```
┌─ キーバインド一覧 ─────────────────────────┐
│ グローバル                                    │
│   ?              この一覧を表示               │
│   q / Ctrl+C     終了 (確認あり)              │
│   Esc            前の画面へ戻る               │
│   Tab/Shift+Tab  タブ切替                     │
│   /              検索                         │
│   r              手動更新                     │
│ 一覧画面共通                                  │
│   j/k, ↓/↑      カーソル移動                 │
│   Enter          詳細/選択                    │
│   a              新規作成                     │
│   e              編集                         │
│   d              削除 (確認ダイアログ)        │
└──────────────────────────────────────────┘
```

### 8.3 キーバインド方針

- 矢印キーと Vim 風 (`j`/`k`/`h`/`l`) の両方をサポートし、`Tab`/`Shift+Tab` でタブ間を移動する。
- `/` でインクリメンタル検索、`d` で削除確認、`x` でセッション切断確認、`Esc` で一段階戻る、`?` でキーバインド一覧をオーバーレイ表示する。
- 破壊的操作 (削除・切断・オフライン化等) は必ず ⑥ のような確認モーダルを経由し、対象名を明示した上で `y`/`n` の明確な二択とする (誤入力防止のため Enter キー単体では確定しない)。

### 8.4 共通 UI パーツ・挙動

- 一覧画面は共通のテーブルコンポーネントを使い、列幅・ソート・フィルタの挙動を統一する。
- 自動更新が有効な画面 (セッション一覧など) は更新間隔と最終更新時刻をステータスバーに表示し、手動リフレッシュ (`r`) にも対応する。
- 画面上部にはブレッドクラム (プロファイル名 / Hub 名 / タブ名) を常時表示し、現在の操作対象を常に把握できるようにする。

## 9. エラーハンドリング / ロギング

- vpncmd の実行失敗 (接続不可、認証失敗、コマンドエラー) はエラーコード・メッセージをそのまま UI に表示しつつ、よくあるケース (パスワード誤り、Hub 未指定、権限不足) は補足説明を付与する。
- アプリ自体の動作ログ (vpncmd 実行コマンド・所要時間・終了コード) はデバッグ用にオプトインでファイル出力できるようにする (パスワードはマスクする)。

## 10. マイルストーン (案)

1. **M1**: 接続プロファイル管理 + 接続テスト + サーバーダッシュボード
2. **M2**: Hub 一覧・作成・削除・基本設定
3. **M3**: ユーザー/グループ管理 (重点機能)
4. **M4**: セッション監視 + ログ設定/閲覧 (重点機能)
5. **M5**: リスナー管理、SecureNAT、アクセスリスト
6. **M6**: カスケード接続・ローカルブリッジ管理 (Bridge モード)
7. **M7**: VPN Client モード対応 (前倒し着手)
8. **M8**: 多言語対応 (i18n) — ロケール自動判定 + `--lang` オプション (6.5 参照。文字化け問題への対応として前倒し着手)
9. **将来**: VPN Tools モード対応、JSON-RPC API 方式の検討

## 11. CI/CD・配布フロー

### 11.1 ビルド・リリース (GitHub Actions)

- タグ push (`v*`, 例: `v0.1.0`) をトリガーに GitHub Actions でリリースワークフローを実行する。
- クロスコンパイルおよびリリース作成には [GoReleaser](https://goreleaser.com/) を利用し、`.goreleaser.yaml` でビルドターゲット (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) とアーカイブ形式を定義する。
- バージョン・コミットハッシュ・ビルド日時は `-ldflags "-X main.version=... -X main.commit=... -X main.date=..."` でバイナリに埋め込み、TUI 上 (バージョン表示コマンドや起動画面) から確認できるようにする。
- リリース成果物:
  - OS/アーキテクチャごとのアーカイブ (例: `softether-tui_linux_amd64.tar.gz`, `softether-tui_windows_amd64.zip`)
  - `checksums.txt` (SHA-256) を同梱し、ダウンロードしたバイナリの検証を可能にする。
- PR / main への push 時は別ワークフローで `go build` / `go vet` / `go test` / lint (`golangci-lint`) を実行し、リリースワークフローとは分離する。

### 11.2 ローカルでの (クロス) コンパイル (Makefile)

- リポジトリ直下の `Makefile` に、CI/GoReleaser と同じビルドオプション (バージョン ldflags、クロスコンパイル対象 OS/アーキテクチャ) をローカルで再現できるターゲットを用意する。GoReleaser を都度実行しなくても、開発中に手元でクロスビルドを試せるようにすることが目的。
- 主なターゲット:
  - `make build`: 現在の (または `GOOS`/`GOARCH` を上書きした) プラットフォーム向けに1つビルドする。
  - `make cross`: `.goreleaser.yaml` と同じ対象 (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) を `dist/` に一括ビルドする。
  - `make checksums`: `dist/` の成果物から `checksums.txt` を生成する。
  - `make check`: `gofmt` / `go vet` / `go test` をまとめて実行する (CI の `ci.yml` と同じ検証)。
  - `make lint` / `make install` / `make run` / `make clean` / `make help`。
- バージョン情報 (`VERSION`/`COMMIT`/`DATE`) は環境変数で上書き可能な Makefile 変数とし、既定値は `git describe`/`git rev-parse`/`date` から自動算出する (git 管理外の場合は `dev`/`none` にフォールバック)。`-ldflags` で `main.version`/`main.commit`/`main.date` に注入する点は 11.1 の GitHub Actions 版と同一の仕組みを共有する。

### 11.3 curl によるインストール

- リポジトリに `install.sh` を用意し、以下の一行コマンドで最新リリースを取得・実行できるようにする:

  ```sh
  curl -fsSL https://raw.githubusercontent.com/<org>/<repo>/main/install.sh | sh
  ```

- `install.sh` の役割:
  1. `uname -s` / `uname -m` で OS・アーキテクチャを判定。
  2. GitHub Releases API (`https://api.github.com/repos/<org>/<repo>/releases/latest`) から最新バージョンのダウンロード URL を取得。
  3. 対応するアーカイブを `curl` で取得し、展開後に実行権限を付与。
  4. `checksums.txt` と照合してダウンロード内容を検証。
  5. `$HOME/.local/bin` (または `sudo` 実行時は `/usr/local/bin`) にバイナリを配置。PATH 未設定の場合は追記方法を案内するメッセージを表示。
- バージョン固定インストール用に `VERSION=v0.1.0 curl ... | sh` のように環境変数でバージョン指定できるようにする。
- `curl | sh` パターンはスクリプト内容を事前に確認せず実行するリスクがあるため、README にスクリプトの中身を確認する手順 (`curl -fsSL .../install.sh -o install.sh && less install.sh` 等) も併記し、利用者が内容を確認してから実行できるようにする。
- 単一バイナリを直接 `curl` で取得して実行するショートカット (アーカイブ展開不要) も、既知の固定 URL パターン (例: `.../releases/latest/download/softether-tui_<os>_<arch>`) で提供し、`install.sh` を介さない最小限の利用方法としても案内する。

## 12. 未決事項 (Open Questions)

- アプリ名称は仮称なし。正式名称を別途決定する (リポジトリ名 `softether-tui` を仮のバイナリ名・リリース成果物名とする)。
- ログ閲覧機能でリモートサーバーのログファイルにどこまでアクセスするか (SSH 経由でのファイル取得を許容するか、vpncmd 経由で取得できる範囲に留めるか) は要検討。
- 複数プロファイルの同時監視 (マルチペイン/タブ) の必要性は運用しながら判断する。
- リリース成果物の署名 (cosign 等によるサプライチェーン署名) を導入するかは将来検討。
- 英語/日本語以外の言語への対応要否は、実際の利用状況を見ながら判断する (6.5 参照)。
- vpncmd 自体の出力 (エラーメッセージ等) を翻訳するかどうかは非対応のまま据え置く。対応する場合 vpncmd 側のロケール設定 (サーバー側 OS ロケール) との整合を別途検討する必要がある。
